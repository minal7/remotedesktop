{
  "name": "remotedesktop-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker signaling room for RemoteDesktop pairing.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:lan": "wrangler dev --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.82.2"
  }
}
